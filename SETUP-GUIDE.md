# دليل تشغيل "بناء البيت" — بالتفصيل، خطوة خطوة

هذا الملف يشرح كل شيء من الصفر. ما أفترض إنك تعرف عن البرمجة أو GitHub أو Supabase — بس أفترض إنك تعرف تفتح المتصفح وتنسخ وتلصق.

خذ وقتك. كل خطوة لها سبب، وأنا أشرح ليش قبل ما أقول كيف.

---

## أولاً: إيش اللي بنيته لك، بلغة بسيطة

فكّر في التطبيق مثل دفتر حسابات ذكي، بس بدل الورق:

- **كل معلومة تنحفظ في مكان اسمه Supabase** — تخيله خزنة أرقام على الإنترنت، تقدر أنت والوالد توصلونها بنفس الوقت.
- **كل صورة وملف (سندات، عقود) تنحفظ في Google Drive** — نفس مجلد Drive العادي اللي تعرفه، بس مشترك بينكما.
- **التطبيق نفسه (الشاشات والأزرار) يعيش في GitHub** — وGitHub عنده خدمة مجانية اسمها Pages تاخذ هذا الكود وتحوّله لموقع تقدر تفتحه من الجوال مثل أي تطبيق.
- **القراءة الذكية للمستندات تستخدم Claude** — نفس الذكاء الاصطناعي اللي تكلمني فيه الحين، بس يشتغل من جهازك مباشرة.

يعني عندك 4 "بيوت" لازم تجهزها قبل ما يشتغل كل شيء مع بعض: **GitHub + Supabase + Google Cloud + مفتاح Claude**. بعدها ما تلمسها مرة ثانية إلا نادراً.

> **إذا تبي بس تجرب التطبيق الحين بدون ما تسوي أي شي من هذا** — روح لقسم "جرّبه أول بدون أي إعداد" تحت. بيشتغل على جهازك فقط، والبيانات ما تنحفظ لأحد غيرك.

---

## قائمة تحقق سريعة (اطبعها أو خلها مفتوحة وأنت تسوي الخطوات)

- [ ] تثبيت Node.js على جهازك
- [ ] فتح مجلد المشروع وتجربته محلياً (demo mode)
- [ ] إنشاء حساب GitHub (إذا ما عندك) + رفع المشروع + تفعيل Pages
- [ ] تشغيل 3 ملفات SQL (001، 002، 004) في نفس مشروع Supabase اللي تستخدمه لتطبيق مصاريف
- [ ] إضافة رمز الدخول لقالب إيميل Supabase
- [ ] تسجيل دخولك ودخول الوالد مرة وحدة في الموقع (حتى لو طلع خطأ)
- [ ] تعديل الإيميلين في ملف seed وتشغيله
- [ ] إعداد Google Cloud (Drive API + Picker + OAuth)
- [ ] إضافة 5 مفاتيح سرية (secrets) في GitHub
- [ ] رفع الكود ليشتغل النشر التلقائي
- [ ] فتح الموقع، اختيار مجلد Drive، إضافة مفتاح Claude
- [ ] "إضافة إلى الشاشة الرئيسية" في جوالك وجوال الوالد

---

## جرّبه أول بدون أي إعداد

هذا يخليك تشوف كل الشاشات وتفهم كيف يشتغل التطبيق، بدون ما تحتاج حساب واحد.

### الخطوة 1: ثبّت Node.js

Node.js هو برنامج صغير يخلي جهازك يقدر "يشغّل" مشروع الويب هذا. مثل ما تحتاج AutoCAD مثبت عشان تفتح ملف dwg، تحتاج Node.js عشان تشغّل هذا المشروع.

1. روح لموقع <https://nodejs.org>
2. حمّل النسخة اللي مكتوب عليها **LTS** (يعني الأكثر استقراراً)
3. شغّل ملف التثبيت واضغط "التالي" على كل شي (الإعدادات الافتراضية تمام)
4. بعد ما يخلص، افتح **PowerShell** (اكتب "PowerShell" في قائمة ابدأ واضغط عليه)
5. اكتب هذا وادخل Enter:
   ```powershell
   node -v
   ```
   إذا طلع لك رقم مثل `v22.x.x` يعني تم التثبيت صح.

### الخطوة 2: افتح المشروع

1. فك ضغط الملف `bina-house-build-manager.zip` اللي عطيتك في أي مكان يعجبك، مثل سطح المكتب.
2. بيصير عندك مجلد اسمه `bina`.
3. في PowerShell، روح لهذا المجلد. مثلاً إذا حطيته في سطح المكتب:
   ```powershell
   cd Desktop\bina
   ```

### الخطوة 3: ثبّت "القطع" اللي يحتاجها المشروع

المشروع مبني من قطع جاهزة (مكتبات برمجية) بدل ما أكتب كل شي من الصفر. هذا الأمر يحمّلها كلها:

```powershell
npm install
```

بيشتغل شوي (دقيقة أو دقيقتين) ويحمّل مجلد اسمه `node_modules`. طبيعي إنه مجلد كبير، لا تحذفه.

### الخطوة 4: شغّله

```powershell
npm run dev
```

بيطلع لك سطر مثل:
```
➜  Local:   http://localhost:5173/
```

افتح هذا الرابط في المتصفح (Chrome مثلاً). بتشوف التطبيق يشتغل! فوق بيكتب "وضع تجريبي على هذا الجهاز" — يعني البيانات اللي بتدخلها تنحفظ بس في هذا المتصفح على هذا الجهاز، ما توصل لأحد.

جرّب تضيف عقد، دفعة، صوّر شي وشوف كيف تصير الشاشات. لما تبي توقفه، ارجع لـ PowerShell واضغط `Ctrl + C`.

---

## الجزء الحقيقي: تجهيزه لك وللوالد، بالإنترنت

الحين نجهز الأربع بيوت اللي ذكرتها فوق. رتّبتها بالترتيب الصح، لا تقفز خطوة.

## 1) GitHub — بيت الكود

GitHub موقع يخزن فيه المبرمجون أكوادهم، وعنده خدمة مجانية تاخذ الكود وتسويه موقع حي تقدر تفتحه. هذا اللي رح يعطيك رابط التطبيق النهائي.

### أ) سوّي حساب

1. روح <https://github.com/signup>
2. سجّل بإيميلك، اختر اسم مستخدم (username) بالإنجليزي
3. أكّد الإيميل

### ب) نزّل GitHub Desktop (أسهل من الأوامر)

بدل ما تتعلم أوامر git المعقدة، فيه برنامج بواجهة رسومية بسيطة:

1. حمّله من <https://desktop.github.com>
2. ثبّته وسجّل دخول بحساب GitHub اللي سويته

### ج) سوّي مستودع (Repository) جديد

المستودع هو مجرد اسم لمجلد المشروع على GitHub.

1. من GitHub Desktop: **File → New Repository**
2. الاسم: `bina` (أو أي اسم تحبه بالإنجليزي، بدون مسافات)
3. **Local Path**: اختر مكان على جهازك — لكن **لا تختار المجلد اللي فيه المشروع الحالي**، خله مجلد فاضي جديد
4. اضغط **Create Repository**

### د) انقل ملفات المشروع للمستودع الجديد

1. من الملف اللي فككت ضغطه (`bina`)، انسخ **كل شي إلا مجلد `node_modules`** (لا تنسخه، كبير وما يحتاج)
2. الصقهم داخل المجلد الجديد اللي سواه GitHub Desktop
3. ارجع لـ GitHub Desktop، بتشوف كل الملفات ظاهرة كـ "تغييرات"
4. تحت، اكتب رسالة بسيطة مثل "أول رفعة" في خانة **Summary**
5. اضغط **Commit to main**
6. فوق اضغط **Publish repository**
   - تأكد إن خيار **"Keep this code private"** مو محدد، أو خله محدد إذا تبي المستودع خاص — الاثنين يشتغلون مع Pages، بس الخاص يحتاج حساب مدفوع أحياناً للميزات المتقدمة. الأسهل: خله **عام (Public)** لأن ما فيه أي معلومة سرية بالكود نفسه (المفاتيح السرية بنحطها بمكان ثاني آمن).

### هـ) فعّل GitHub Pages

1. روح لصفحة المستودع على موقع GitHub (من المتصفح، مو البرنامج)
2. **Settings** (أعلى الصفحة) → من القائمة اليسار **Pages**
3. تحت **Build and deployment** → **Source** اختر **GitHub Actions**
4. خلاص، هذا كل شي هنا. الموقع بيصير:
   ```
   https://<اسم-المستخدم>.github.io/bina/
   ```
   احفظ هذا الرابط، بتحتاجه بعدين.

> ⚠️ الموقع ما بيشتغل صح إلى الحين — ناقصنا نضيف "المفاتيح السرية" (خطوة 8). عادي، كمّل.

---

## 2) Supabase — بيت الأرقام والبيانات

أنت عندك مشروع Supabase شغال أصلاً لتطبيق "مصاريف". رح نستخدم **نفس المشروع**، بس نضيف له جداول جديدة خاصة بهذا التطبيق (أسماء الجداول كلها تبدأ بـ `hb_` عشان ما تتعارض مع مصاريف).

### أ) شغّل ملفات SQL

SQL هي لغة نعطي فيها أوامر لقاعدة البيانات مثل "سوّي جدول اسمه كذا". أنا جهزت لك الأوامر جاهزة، بس تنسخ وتلصق.

1. روح لموقع <https://supabase.com> وسجّل دخول
2. افتح نفس مشروعك (اللي فيه تطبيق مصاريف)
3. من القائمة اليسار اضغط **SQL Editor**
4. اضغط **New query**
5. افتح الملف `supabase/migrations/001_schema.sql` من مجلد المشروع (بأي برنامج نصوص مثل Notepad)
6. انسخ **كل المحتوى** والصقه في مربع SQL Editor
7. اضغط **Run** (أو `Ctrl+Enter`)
8. لازم يطلع لك "Success" تحت. إذا طلع خطأ أحمر، انسخه وأرسله لي

### ب) فعّل ميزة الجدولة (pg_cron) ثم شغّل الملف الثاني

هذا الملف فيه أمر يحذف تلقائياً أي شي في سلة المحذوفات بعد 30 يوم، وهذا يحتاج ميزة اسمها pg_cron.

1. من القائمة اليسار: **Database** → **Extensions**
2. ابحث عن `pg_cron` وفعّله (زر التفعيل)
3. ارجع لـ **SQL Editor** → **New query**
4. افتح `supabase/migrations/002_security_audit.sql`، انسخ كل المحتوى، الصقه، واضغط **Run**

### ب2) شغّل الملف الثالث (الضمانات وسجل الموقع)

نفس الطريقة: **SQL Editor → New query**، افتح `supabase/migrations/004_phase3.sql`، انسخ الكل، الصق، **Run**.

### ج) خلي رابط GitHub Pages مسموح له يدخل

1. من القائمة اليسار: **Authentication** → **URL Configuration**
2. في خانة **Redirect URLs** اضغط **Add URL** وحط:
   ```
   https://<اسم-المستخدم>.github.io/bina/
   ```
   (نفس الرابط اللي حفظته فوق)

### د) خلي رمز الدخول يوصل بالإيميل

عادة Supabase يرسل "رابط دخول" بس. أضفنا خيار كمان يرسل "رمز من 6 أرقام" تكتبه يدوي — مفيد جداً لما تفتح التطبيق من أيقونته بالشاشة الرئيسية (الرابط بالإيميل أحياناً يفتح المتصفح العادي بدل التطبيق).

1. **Authentication** → **Email Templates**
2. اختر **Magic Link** من القائمة
3. بتشوف صندوق فيه كود HTML. أضف هذا السطر في أي مكان مناسب داخل الصندوق (مثلاً بعد الزر الموجود):
   ```html
   <p>أو اكتب هذا الرمز داخل التطبيق: <strong>{{ .Token }}</strong></p>
   ```
4. احفظ (**Save**)

### هـ) سجّلوا دخول أول مرة (أنت والوالد)

هذا يخلي حسابيكم "موجودين" في النظام عشان بعدين نربطكم بالمشروع.

1. بعد ما نخلص خطوة المفاتيح السرية (تحت) وينشر الموقع، افتح الرابط من جوالك
2. اكتب إيميلك واضغط "أرسل رابط الدخول"
3. افتح الإيميل واضغط الرابط، أو ارجع للتطبيق واكتب الرمز المكوّن من 6 أرقام
4. بيطلع لك شاشة تقول "حسابك غير مضاف للمشروع" — **هذا متوقع تماماً**، لا تقلق
5. خلّي نفس الشي يسويه الوالد من جواله بإيميله

### و) اربطوا الحسابين بمشروع البيت

1. ارجع لـ Supabase → **SQL Editor** → **New query**
2. افتح ملف `supabase/migrations/003_seed.sql`
3. بداخله بتشوف مكانين مكتوب فيهم:
   ```
   AHMED_EMAIL@example.com
   FATHER_EMAIL@example.com
   ```
   بدّلهم بإيميلك الحقيقي وإيميل الوالد الحقيقي (نفس الإيميلين اللي استخدمتوهم في الخطوة السابقة بالضبط)
4. انسخ المحتوى كامل، الصقه في SQL Editor، واضغط **Run**
5. ارجعوا لتطبيق (أعيدوا تحميل الصفحة / أعيدوا فتح التطبيق) — الحين المفروض يفتح على الشاشة الرئيسية مباشرة 🎉

---

## 3) Google Cloud — بيت ملفات Drive

هذا الجزء الأصعب شوي، بس نسويه مرة وحدة وخلاص. الهدف: نعطي التطبيق إذن يرفع الملفات لمجلد Drive معيّن (بس ذاك المجلد، ما يشوف باقي ملفاتك).

### أ) سوّي مشروع في Google Cloud

1. روح <https://console.cloud.google.com>
2. سجّل دخول بنفس حساب Google اللي فيه Drive تبعك
3. فوق يسار، اضغط على اسم المشروع (أو "Select a project") → **New Project**
4. اسمه مثلاً `بناء البيت` أو `bina-house` → **Create**
5. تأكد إن المشروع الجديد هو المختار فوق

### ب) فعّل الخدمتين المطلوبتين

1. من القائمة (☰) → **APIs & Services** → **Library**
2. ابحث عن **Google Drive API** → افتحه → اضغط **Enable**
3. ارجع لـ Library، ابحث عن **Google Picker API** → افتحه → اضغط **Enable**

### ج) جهّز "شاشة الموافقة" (OAuth consent screen)

هذي الشاشة اللي بتطلع لك ولوالدك أول مرة تسجلون فيها دخول بـ Google وتوافقون إن التطبيق يوصل Drive.

1. من القائمة → **APIs & Services** → **OAuth consent screen**
2. **User Type**: اختر **External** → **Create**
3. عبّي:
   - App name: `بناء البيت`
   - User support email: إيميلك
   - Developer contact: إيميلك
4. استمر (Save and Continue) على باقي الصفحات بدون ما تغيّر شي، إلى إن توصل لصفحة **Test users**
5. في **Test users** اضغط **Add Users** وأضف:
   - إيميلك
   - إيميل الوالد
6. كمّل واحفظ

> ليش "Test users"؟ لأن التطبيق شخصي بينكم الاثنين بس، ما نحتاج نرسله لمراجعة Google الرسمية (اللي تاخذ أسابيع). بوضع "اختبار" يشتغل بشكل دائم لأي إيميل تضيفه في هذي القائمة.

### د) سوّي مفتاح OAuth (Client ID)

هذا المفتاح اللي يقول لـ Google "هذا التطبيق مسموح له يطلب صلاحية Drive".

1. **APIs & Services** → **Credentials**
2. **Create Credentials** → **OAuth client ID**
3. Application type: **Web application**
4. الاسم: أي شي، مثلاً `bina web`
5. تحت **Authorized JavaScript origins** اضغط **Add URI** وأضف الاثنين:
   ```
   https://<اسم-المستخدم>.github.io
   http://localhost:5173
   ```
   (لاحظ بدون `/bina` وبدون `/` في الآخر)
6. اضغط **Create**
7. بيطلع لك مربع فيه **Client ID** — انسخه واحفظه بملف نصوص مؤقت، بتحتاجه بعد شوي

### هـ) سوّي مفتاح Picker (API key)

1. نفس صفحة **Credentials** → **Create Credentials** → **API key**
2. ينسخ لك مفتاح مباشرة — احفظه
3. (اختياري بس أفضل) اضغط **Edit API key** بجنبه → تحت **API restrictions** اختر **Restrict key** وحدد **Google Picker API** بس → **Save**

### و) احفظ رقم المشروع (App ID)

1. من القائمة → **Home** (الصفحة الرئيسية لـ Cloud Console)
2. بتشوف بطاقة **Project info** فيها **Project number** — رقم، مثل `123456789012`
3. احفظه، هذا هو الـ App ID

الحين عندك 3 أشياء محفوظة: **Client ID**، **API key**، **Project number**.

---

## 4) مفتاح Claude (اختياري، بس هو اللي يشغّل القراءة الذكية)

هذا ما تحطه في إعدادات GitHub — تحطه **داخل التطبيق نفسه** بعد ما يشتغل، من "الإعدادات → مفتاح Claude API"، وينحفظ على كل جهاز لحاله. يعني أنت والوالد كل واحد يضيف مفتاحه هو (أو نفس المفتاح إذا تبون).

احصل عليه من <https://console.anthropic.com> → API Keys → Create Key. هذا مفتاح مختلف تماماً عن اشتراك Claude.ai العادي، له فوترة منفصلة حسب الاستخدام (بسيطة جداً لعدد صور قليل بالأسبوع).

---

## 5) رجعة لـ GitHub: خلي المفاتيح السرية توصل للموقع

الحين نجمع كل المفاتيح اللي جمعناها (Supabase + Google) ونحطها بمكان آمن داخل GitHub، عشان الموقع يقدر يستخدمها بدون ما تكون مكتوبة بشكل ظاهر بالكود.

### وين تلقى مفاتيح Supabase

1. روح لمشروعك في Supabase → **Settings** (⚙️) → **API**
2. بتلقى:
   - **Project URL** (رابط يبدأ بـ `https://xxxxx.supabase.co`)
   - **anon public** key (سطر طويل)

### أضف كل شي في GitHub

1. روح لصفحة المستودع على GitHub (بالمتصفح)
2. **Settings** → من القائمة اليسار **Secrets and variables** → **Actions**
3. اضغط **New repository secret** لكل واحد من هذي الخمسة (تكرر الخطوة 5 مرات):

| الاسم (اكتبه بالضبط) | وينه |
| --- | --- |
| `VITE_SUPABASE_URL` | Project URL من Supabase |
| `VITE_SUPABASE_ANON_KEY` | anon public key من Supabase |
| `VITE_GOOGLE_CLIENT_ID` | Client ID من Google Cloud |
| `VITE_GOOGLE_API_KEY` | API key من Google Cloud |
| `VITE_GOOGLE_APP_ID` | Project number من Google Cloud |

كل مرة: اكتب الاسم بالضبط كما هو بالجدول، الصق القيمة، اضغط **Add secret**.

### شغّل النشر

الآن لازم "نحرّك" GitHub عشان يعيد بناء الموقع بالمفاتيح الجديدة:

1. من GitHub Desktop، سوّي أي تغيير بسيط (مثلاً افتح ملف README.md وأضف مسافة واحفظ)، أو ببساطة:
2. روح لصفحة المستودع على GitHub → تبويب **Actions**
3. من القائمة اليسار اختر **Deploy to GitHub Pages**
4. اضغط **Run workflow** → **Run workflow** (زر أخضر)
5. انتظر دقيقة أو دقيقتين، لين تشوف علامة ✅ خضراء

خلاص! افتح رابط GitHub Pages تبعك من جوالك، والمفروض يفتح شاشة تسجيل الدخول الحقيقية.

---

## 6) آخر لمسة: من داخل التطبيق

بعد ما تسجلون دخول (خطوة 2-هـ و2-و فوق):

1. **من جهازك (أحمد):**
   - افتح **الإعدادات → مجلد Google Drive → إنشاء مجلد جديد**
   - وافق على صلاحية Google لما يطلبها
   - روح لـ Google Drive العادي (drive.google.com)، دور على مجلد اسمه "بناء البيت"، بالزر اليمين **مشاركة** → أضف إيميل الوالد كـ **محرر (Editor)**
2. **من جهاز الوالد:**
   - **الإعدادات → مجلد Google Drive → اختيار مجلد موجود**
   - في النافذة اللي تطلع فيها تبويبين، اضغط التبويب الثاني (الملفات المشتركة معي) ودور على "بناء البيت" واختره
3. كل واحد منكم: **الإعدادات → مفتاح Claude API** → الصق مفتاحك
4. من متصفح الجوال (Safari أو Chrome): زر المشاركة → **إضافة إلى الشاشة الرئيسية** — الحين صار عندكم أيقونة مثل أي تطبيق عادي

---

## طيب، كيف أستخدمه يومياً؟

- **زر "+" الأزرق تحت يمين:** يفتح لك قائمة — صوّر مستند / دفعة / شراء مباشر / عقد جديد / بند متوقع / أمر تغيير
- **أسهل طريقة:** اضغط "صوّر مستند" وصوّر أي سند أو فاتورة أو عقد — التطبيق يحاول يعبّي البيانات لك وتراجعها بس قبل الحفظ
- **الرئيسية:** تشوف فيها التكلفة المتوقعة الكلية، وأي دفعة بدون سند محتاجة صورة، وآخر إضافات الوالد
- **الجهات:** كل مقاول أو مورد وكم دفعت له وكم باقي
- **المستندات:** كل الصور والملفات، تقدر تفلترها
- **الملخص:** رسم بياني للمصروف حسب البند وحسب الشهر
- **سجل الموقع:** من زر "+" اختر "زيارة موقع"، صوّر تقدم البناء واكتب ملاحظة. تنعرض كخط زمني حسب المرحلة
- **الضمانات:** من زر "+" اختر "ضمان". قبل انتهاء أي ضمان بشهرين يطلع لك تنبيه في الرئيسية
- **المحتجز:** إذا العقد فيه نسبة محتجز، اكتبها في تفاصيل العقد. كل دفعة بعدها تحسب لك المحتجز تلقائياً، ولما ينتهي وقت الصيانة اضغط "تسجيل الإفراج" من صفحة العقد
- **حذفت شي بالغلط؟** كل حذف يعطيك زر "تراجع" فوري، وحتى لو سكرت التطبيق تلقاه في **الإعدادات → سلة المحذوفات** لمدة 30 يوم

---

## مشاكل شائعة وحلولها

**"حسابك غير مضاف للمشروع" ما زالت تطلع بعد تشغيل 003_seed.sql**
تأكد إن الإيميل اللي كتبته بملف seed مطابق **حرف بحرف** للإيميل اللي سجلت فيه دخول (بدون مسافات زايدة). جرّب تسجل خروج ودخول من جديد.

**زر "اختيار مجلد Drive" ما يشتغل أو يطلع خطأ**
تأكد إن إيميلك مضاف في **Test users** بصفحة OAuth consent screen بالضبط بنفس الحساب اللي فاتح فيه Drive.

**القراءة الذكية تقول "مفتاح غير صحيح"**
تأكد إنك نسخت المفتاح كامل من console.anthropic.com بدون مسافة زايدة بالأول أو الآخر.

**الموقع يطلع صفحة فاضية أو خطأ بعد فتح الرابط**
روح لتبويب **Actions** بصفحة GitHub وشوف آخر تشغيلة (آخر سطر)، إذا فيها ❌ احمر اضغط عليها وشوف رسالة الخطأ، أو أرسلها لي وأساعدك.

**نسيت رابط GitHub Pages تبعك**
هو دائماً: `https://<اسم-المستخدم-تبعك>.github.io/bina/`

---

إذا وصلت لأي خطوة وتعثرت، انسخ لي رسالة الخطأ بالضبط أو قول لي وين وقفت وبساعدك أكمل.
